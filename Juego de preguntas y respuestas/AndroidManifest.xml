<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ejemplo.quizgame">

    <application
        android:name=".App"  <!-- Especifica una clase Application si la tienes; si no, elimina esta línea. -->
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"  <!-- Especifica reglas de extracción de datos si las tienes. -->
        android:fullBackupContent="@xml/backup_rules"  <!-- Especifica reglas para el backup de contenido. -->
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoParcial"  <!-- Usa el tema que definiste en tu proyecto. -->
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"  <!-- Asegura que esta actividad sea accesible desde fuera en Android 12+. -->
            android:label="@string/app_name"
            android:theme="@style/Theme.ProyectoParcial">  <!-- Usa el mismo tema aquí para mantener consistencia. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>

